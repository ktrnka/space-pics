# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env

# Downloaded image cache (feeds and candidates are committed; images are re-fetched)
data/images/

# Jekyll
site/_site/
site/.jekyll-cache/
site/.jekyll-metadata
site/vendor/
site/.bundle/

# Scratch
tmp/

# Locally generated images (composites); the published copy is under site/assets/img/
data/derived/

# data/survey/ (exploration fetches of other dates) is committed so CI explorer pages match local ones.

# Experiment outputs and scratch venv
experiments/out/
experiments/.venv/

# Local-only docs (third-party copies, private notes); not published
docs/local/

# Per-task git worktrees and their review packages (see CLAUDE.md, Parallel work)
.claude/worktrees/
scratch/
review/
